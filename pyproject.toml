[project]
name = "spearfishing-visi-tide-api"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.110",
    "httpx>=0.27",
    "pydantic>=2.6",
    "uvicorn[standard]>=0.29",
]

[tool.vercel]
entrypoint = "backend.app.main:app"
